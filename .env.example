# Environment is split by app:
#
#   backend/.env.example  → copy to backend/.env   (Python API + integration tests)
#   frontend/.env.example → copy to frontend/.env.local (Next.js; when you add the app)
#
# Do not keep a repo-root `.env` — backend reads only `backend/.env` (or BACKEND_ENV_FILE).
